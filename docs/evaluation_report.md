# Evaluation Report

Run:

```bash
python evaluation/run_all.py
```

The latest generated report is written to:

```text
evaluation/reports/latest_report.md
```

Do not copy benchmark numbers into README until the report has been produced on the target machine with the intended model, data source, and API key.

